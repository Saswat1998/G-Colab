{
  "name": "g-colab",
  "version": "1.0.0",
  "description": "Collaborative document editing tool",
  "main": "api.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && lambda-zipper build -f ./dist -o build/lambda.zip"
  },
  "author": "Saswat",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.11.19",
    "aws-sdk": "^2.1560.0",
    "lambda-zipper": "^1.0.0",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.133"
  }
}
